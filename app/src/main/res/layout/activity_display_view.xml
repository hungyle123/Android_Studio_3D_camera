<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/display_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DisplayView">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/footer_bar"/>

    <RelativeLayout
        android:id="@+id/footer_bar"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:background="#195eb6">

        <ImageView
            android:id="@+id/go_back_button"
            android:layout_width="50dp"
            android:layout_height="25dp"
            android:src="@drawable/ic_double_arrow_left"
            android:layout_centerVertical="true"
            android:layout_alignTop="@+id/footer_title"
            android:layout_alignBottom="@+id/footer_title"
            android:padding="5dp" />

        <TextView
            android:id="@+id/footer_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Chọn cách hiển thị"
            android:textSize="30sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:layout_centerInParent="true"/>
    </RelativeLayout>

</RelativeLayout>